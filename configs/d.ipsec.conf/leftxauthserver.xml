  <varlistentry>
  <term><emphasis>leftxauthserver</emphasis></term>
  <listitem>
<para>Left is an XAUTH server. This can use PAM for authentication or md5 passwords
in <emphasis>/etc/ipsec.d/passwd</emphasis>. These are additional credentials
to verify the user identity, and should not be confused with the XAUTH
<emphasis>group secret</emphasis>, which is just a regular PSK defined in
<emphasis>ipsec.secrets</emphasis>.  The other side of the connection
should be configured as <emphasis>rightxauthclient</emphasis>. XAUTH
connections cannot rekey, so <emphasis>rekey=no</emphasis> should be
specified in this conn.  For further details on how to compile and use XAUTH, see README.XAUTH.
Acceptable values are <emphasis>yes</emphasis> or <emphasis>no</emphasis>
(the default).
</para>
  </listitem>
  </varlistentry>

