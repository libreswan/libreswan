<varlistentry>
  <term>
    <emphasis remap='B'>intermediate</emphasis>
  </term>
  <listitem>
    <para>
      EXPERIMENTAL: Whether to enable the INTERMEDIATE exchange in
      IKEv2 (RFC 9242).  Currently the accepted values are <emphasis
      remap='B'>yes</emphasis>, signifying we propose to use the
      INTERMEDIATE exchange for this connection; <emphasis
      remap='B'>no</emphasis> (the default), signifying we will not
      use the INTERMEDIATE exchange for this connection.
    </para>
  </listitem>
</varlistentry>
