<varlistentry id='conn.aggressive'>
  <term>
    <option>aggressive=&yn_option;</option>
  </term>
  <listitem>

    <para>
      For &IKEv1; connections, use Aggressive Mode instead of Main
      Mode.  When this option is enabled, &IKEv1; Main Mode will no
      longer be allowed for this connection.
    </para>

    <para>
      Aggressive Mode is less secure, and more vulnerable to Denial Of
      Service attacks. It is also vulnerable to brute force attacks
      with software such as <command>ikecrack</command>.  It should
      not be used, and it should especially not be used with XAUTH and
      group secrets (PSK).  If the remote system administrator insists
      on staying irresponsible, enable this option.
    </para>

    <para>
      Aggressive Mode is further limited to only proposals with one DH
      group as there is no room to negotiate the DH group. Therefore
      it is mandatory for Aggressive Mode connections that both
      <option>ike=</option> and <option>esp=</option> options are
      specified with only one fully specified proposal using one DH
      group.
    </para>

    <para>
      The KE payload is created in the first exchange packet when
      using aggressive mode. The KE payload depends on the DH group
      used.  This is why there cannot be multiple DH groups in &IKEv1;
      aggressive mode.  In &IKEv2;, which uses a similar method to
      &IKEv1; Aggressive Mode, there is an INVALID_KE response payload
      that can inform the initiator of the responder's desired DH
      group and so an &IKEv2; connection can actually recover from
      picking the wrong DH group by restarting its negotiation.
    </para>

    <para>
      History: the parameter was originally called
      <option>aggrmode=</option>; it was was renamed to
      <option>aggressive=</option> in &Libreswan; 3.23.
    </para>

    <para>
      The default is <option>aggressive=&no;</option>.
    </para>

  </listitem>
</varlistentry>
