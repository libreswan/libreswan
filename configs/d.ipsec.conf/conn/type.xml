<varlistentry id='conn.type'>
  <term>
    <option>type=&type_option;</option>
  </term>
  <listitem>
    <para>
      the type of the connection; currently the accepted values are
      &tunnel;, signifying a host-to-host, host-to-subnet, or
      subnet-to-subnet tunnel; &transport;, signifying host-to-host
      transport mode; &passthrough;, signifying that no IPsec
      processing should be done at all; &drop;, signifying that
      packets should be discarded.
    </para>
    <para>
      The default is &tunnel;.
    </para>
    <para>
      Historic Note: &KLIPS; supported the option
      <option>reject</option> signifying that packets should be
      discarded and a diagnostic ICMP returned.  Support for &KLIPS;,
      and this option, have been removed.
    </para>
  </listitem>
</varlistentry>
