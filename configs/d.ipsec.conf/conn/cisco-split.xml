<varlistentry id='conn.cisco-split'>
  <term>
    <option>cisco-split=&yn_option;</option>
  </term>
  <listitem>
    <para>
      Use the IKEv1 MODE_CFG CISCO_SPLIT_INCLUDE payload to transfer
      the server's <option>subnet=</option> list to the client.
      During the Quick Mode exchange, the client and server will then
      install kernel policy that includes all the subnets.
    </para>
    <para>
      The default is &no;.
    </para>

    <para>
      History: <option>cisco-split</option> was added in &Libreswan;
      5.3.
    </para>

  </listitem>
</varlistentry>
