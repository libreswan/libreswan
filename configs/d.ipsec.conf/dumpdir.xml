<varlistentry>
  <term>
    <emphasis>dumpdir</emphasis>
  </term>
  <listitem>
    <para>
      in what directory should things started by <emphasis>setup</emphasis>
      (notably the Pluto daemon) be allowed to dump core?  The default value is
      <emphasis>@@RUNDIR@@</emphasis>. When SELinux runs in enforced mode,
      changing this requires a similar change in the SELinux policy for
      the pluto daemon.
    </para>

  </listitem>
</varlistentry>
