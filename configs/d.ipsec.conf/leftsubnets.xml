<varlistentry>
  <term>
    <emphasis remap='B'>leftsubnets</emphasis>
  </term>
  <listitem>
    <para>
      specify multiple private subnets behind the left participant,
      expressed as { <emphasis remap='I'>networkA</emphasis><emphasis
      remap='B'>/</emphasis><emphasis remap='I'>netmaskA</emphasis>,
      <emphasis remap='I'>networkB</emphasis><emphasis
      remap='B'>/</emphasis><emphasis remap='I'>netmaskB</emphasis>
      <emphasis remap='I'>[...]</emphasis> } If both a leftsubnets=
      and rightsubnets= are defined, all combinations of subnet
      tunnels will be established as IPsec tunnels. You cannot use
      leftsubnet= and leftsubnets= together. For examples see
      <emphasis remap='I'>testing/pluto/multinet-*</emphasis>. Be
      aware that when using spaces as separator, that the entire
      option value needs to be in double quotes.
    </para>

    <important>
      <title>
	<code>leftsubnet</code>
      </title>
      <para>
	Combining a <code>leftsubnet</code> specifying either multiple
	selectors or selectors that specify the protocol and port with
	<code>leftsubnets</code> is not supported.
      </para>
    </important>

  </listitem>
</varlistentry>
