<refsect1 id='config_sections'>
  <title>
    CONFIG SECTIONS
  </title>
  <para>
    At present, the only <emphasis>config</emphasis> section
    known to the IPsec software is the one named <emphasis>setup</emphasis>,
    which contains information used when the software is being started.
  </para>

  <programlisting><xi:include  href="configsections.example"  parse="text"
  xmlns:xi="http://www.w3.org/2001/XInclude"/></programlisting>

  <para>
    Parameters are optional unless marked &ldquo;(required)&rdquo;.
  </para>

  <para>
    The currently-accepted <emphasis>parameter</emphasis>
    names in a <emphasis>config setup</emphasis> section
    are:
  </para>

  <variablelist>
