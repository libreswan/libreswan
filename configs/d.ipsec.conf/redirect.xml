  <varlistentry>
  <term><option>accept-redirect</option></term>
  <listitem>
<para>Whether requests of the remote peer to redirect IKE/IPsec SA's are accepted. Valid options are
	<option>no</option> (the default) and <option>yes</option>. See also <option>accept-redirect-to</option>.
</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>accept-redirect-to</option></term>
  <listitem>
<para>
Specify the comma separated list of addresses we accept being
redirected to. Both IPv4 and IPv6 addresses
are supported as well the FQDNs. The value
<option>%any</option>, as well as not specifying
any address, signifes that we will redirect to any address
gateway sends us in REDIRECT notify payload.
</para>
<para>
The value of this option is not considered at all if
accept-redirect is set to no.
</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>send-redirect</option></term>
  <listitem>
<para>Whether to send requests for the remote peer to redirect
IKE/IPsec SA's during IKE_AUTH. Valid options are
<option>no</option> (the default) and <option>yes</option>. If
set, the option <option>redirect-to=</option> must also be set to
indicate where to redirect peers to. For redirection during
IKE_SA_INIT exchange, see the <option>global-redirect=</option>
and <option>global-redirect-to=</option> options. Runtime
redirects can be triggered via the <command>ipsec redirect</command>
command.
</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>redirect-to</option></term>
  <listitem>
	  <para>Where to send remote peers to via the <option>send-redirect</option> option. This can be an IP address or hostname (FQDN).
</para>
  </listitem>
  </varlistentry>
