<varlistentry id='setup.ocsp-uri'>
  <term>
    <option>ocsp-uri=&URI;</option>
  </term>
  <listitem>
    <para>
      The URI to use for OCSP requests instead of the default OCSP URI
      listed in the CA certificate.  This requires the
      <option>ocsp-trustname</option> option to be set to the nick
      (friendly name) of the OCSP server certificate, which needs to
      be present in the NSS database.  These option combined with the
      next option sets the OCSP <option>default responder</option>.
    </para>
  </listitem>
</varlistentry>
