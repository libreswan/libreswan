<varlistentry id='setup.statsbin'>
  <term>
    <option>statsbin=</option>
  </term>
  <listitem>
    <para>
      This option specifies an optional external program to report
      tunnel state changes too.  This program can be used to notify
      the user's desktop (dbus, NetworkManager) or to report tunnel
      changes to a central logging server.
    </para>
    <para>
      The default is to not report tunnel state changes.
    </para>
  </listitem>
</varlistentry>
