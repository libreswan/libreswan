<varlistentry>
  <term>
    <emphasis>keyexchange</emphasis>
  </term>
  <listitem>
    <para>
      Whether to use IKEv2 (RFC 7296) or IKEv1 (RFC 4301). Currently the
      accepted values are <emphasis>ikev2</emphasis> (the default) and
      <emphasis>ikev1</emphasis>.
    </para>
    <para>
      This option replaces ikev2=yes|no. Old values yes, propose and instist
      all map to keyexchange=ikev2. Values no and permit are map to
      keyexchange=ikev1.
    </para>
  </listitem>
</varlistentry>

