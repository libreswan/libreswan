<varlistentry>
  <term>
    <emphasis remap='B'>ipsec-max-packets</emphasis>
  </term>
  <listitem>
    <para>
      how many packets can be sent/received on a particular instance
      of a connection (a set of encryption/authentication keys for
      user packets) , from successful negotiation to expiry.
    </para>
    <para>
      Default values and caveats are the same as for <emphasis
      remap='B'>ipsec-max-bytes</emphasis>.  This option uses a prefix
      without "B" for bytes.
    </para>
  </listitem>
</varlistentry>
