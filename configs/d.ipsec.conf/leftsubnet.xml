<varlistentry>
  <term>
    <emphasis remap='B'>leftsubnet</emphasis>
  </term>
  <listitem>
    <para>
      Private subnet behind the left participant, expressed as
      <emphasis remap='I'>network</emphasis><emphasis
      remap='B'>/</emphasis><emphasis remap='I'>netmask</emphasis>
    </para>
    <para>
      A comma separated list of traffic selectors behind the left
      participant.  Each expressed as
      <replaceable>network-prefix</replaceable> <emphasis>/</emphasis>
      <replaceable>netmask</replaceable> <emphasis>/</emphasis>
      <replaceable>protocol</replaceable> <emphasis>/</emphasis>
      <replaceable>port</replaceable> where trailing elements may be
      omitted.
    </para>
    <screen>
      1.2.3.4/24
      1.2.3.4/32/tcp
      1.2.3.4/31/tcp/22
      1:2::/64
      1:2::/128/tcp
      1:2::/128/tcp/22
    </screen>
    <para>
      When omitted, essentially assumed to be <emphasis
      remap='I'>left</emphasis>, signifying that the left end of the
      connection goes to the left participant only.
    </para>
    <para>
      In addition, IKEv1 supports two magic shorthands <emphasis
      remap='I'>vhost:</emphasis> and <emphasis
      remap='I'>vnet:</emphasis>, which can list subnets in the same
      syntax as <emphasis remap='I'>virtual-private</emphasis>.  The
      value <emphasis remap='I'>%priv</emphasis> expands to the
      networks specified in <emphasis
      remap='I'>virtual-private</emphasis>.  The value <emphasis
      remap='I'>%no</emphasis> means no subnet. A common use for
      allowing roadwarriors to come in on public IPs or via accepted
      NATed networks from RFC1918 is to use <emphasis
      remap='I'>leftsubnet=vhost:%no,%priv</emphasis>. The <emphasis
      remap='I'>vnet:</emphasis> option can be used to allow RFC1918
      subnets without hardcoding them.  When using vnet the connection
      will instantiate, allowing for multiple tunnels with different
      subnets.
    </para>
  </listitem>
</varlistentry>
