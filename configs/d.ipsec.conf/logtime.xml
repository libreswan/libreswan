  <varlistentry>
  <term><emphasis>logtime</emphasis></term>
  <listitem>
<para>When pluto is directed to log to a file using <emphasis>logfile=</emphasis>,
this option determines whether or not to log the current timestamp as prefix.
Values are <emphasis>yes</emphasis> (the default) or
<emphasis>no</emphasis>. The no value can be used to create logs without
ephemeral timestamps, such as those created when running the test suite.
This option used to be called plutostderrlogtime=
</para>
  </listitem>
  </varlistentry>
