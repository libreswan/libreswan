# /etc/ipsec.conf - Openswan IPsec configuration file
# RCSID $Id: ipsec.conf.in,v 1.16 2005/07/26 12:29:45 ken Exp $

# This file:  @FINALEXAMPLECONFDIR@/ipsec.conf-sample
#
# Manual:     ipsec.conf.5


version	2.0	# conforms to second version of ipsec.conf specification

# basic configuration
config setup
	# plutodebug / klipsdebug = "all", "none" or a combation from below:
	# "raw crypt parsing emitting control klips pfkey natt x509 private"
	# eg:
	# plutodebug="control parsing"
	#
	# Only enable *debug=all if you are a developer
	#
	# NAT-TRAVERSAL support, see README.NAT-Traversal
	nat_traversal=yes
	# exclude networks used on server side by adding %v4:!a.b.c.0/24
	virtual_private=%v4:10.0.0.0/8,%v4:192.168.0.0/16,%4:172.16.0.0/12
	# If you see errors about helpers/workers, tryg uncommenting this
	#nhelpers=0
	# OE is now off by default. Change to on to enable.
	OE=off
	# For IPsec SAref, we need to use the MAST (KLIPSNG) stack
	protostack=mast
	

# Add connections here

## sample VPN connection
#conn l2tp
#        # Configuration for one user with any type of IPsec/L2TP client
#        # including the updated Windows 2000/XP (MS KB Q818043), but
#        # excluding the non-updated Windows 2000/XP.
#        #
#        #
#        # Use a certificate. Disable Perfect Forward Secrecy.
#        #
#        authby=rsasig
#        pfs=no
#        auto=add
#        rekey=no
#        type=transport
#	# Allow overlapping IP addresses on connecting clients
#        overlapip=yes
#	#
#	# The local server
#	#
#        leftsendcert=always
#	# Make sure the interface with the public ip has an mtu of 1472
#        left=YourPublicIP
#	leftnexthop=IPofGateway
#        leftrsasigkey=%cert
#        leftcert=/etc/ipsec.d/certs/yourserver.cert
#        leftprotoport=17/1701
#        #
#        # The remote user.
#        #
#        right=%any
#        rightca=%any
#        rightrsasigkey=%cert
#        rightprotoport=17/0
#        #rightprotoport=17/%any
#        rightsubnet=vhost:%priv,%no
#
