# this script was generated by ./testing/utils/check-10.sh

east# ../../guestbin/ip.sh addr show eth0
east# ../../guestbin/ip.sh link set eth0 up
east# ../../guestbin/ip.sh addr show eth1
east# ../../guestbin/ip.sh link set eth1 up

west# ../../guestbin/ip.sh addr show eth0
west# ../../guestbin/ip.sh link set eth0 up
west# ../../guestbin/ip.sh addr show eth1
west# ../../guestbin/ip.sh link set eth1 up

rise# ip link set eth1
rise# ip link set eth1 up
rise# ip link set eth0
rise# ip link set eth0 up

set# ip link set eth1
set# ip link set eth1 up
set# ip link set eth0
set# ip link set eth0 up

rise# ../../guestbin/ping-once.sh --up 198.18.1.15  # SET
rise# ../../guestbin/ping-once.sh --up 192.0.2.254  # EAST

set# ../../guestbin/ping-once.sh --up 198.18.1.12  # RISE
set# ../../guestbin/ping-once.sh --up 192.0.1.254  # WEST

rise# ../../guestbin/prep.sh
rise# ipsec initnss
rise# ipsec pluto --selftest

set# ../../guestbin/prep.sh
set# ipsec initnss
set# ipsec pluto --selftest
