# /etc/ipsec.conf - Libreswan IPsec configuration file

version 2.0

config setup
	ikev1-policy=accept
	logfile=/tmp/pluto.log
	plutodebug=all
	dumpdir=/tmp

conn %default
	keyexchange=ikev1

# no conns loaded or started
