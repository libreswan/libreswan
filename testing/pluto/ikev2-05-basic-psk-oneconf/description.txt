Basic pluto with IKEv2 using PSK on the initiator (west), and on the responder.

This is a meta-test case not meant to run in KVM. It is for testing a setup using NAMESPACES

It is using netkey because klips does not support NAMESPACES
