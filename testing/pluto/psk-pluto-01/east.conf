# /etc/ipsec.conf - Libreswan IPsec configuration file

version 2.0

config setup
	# put the logs in /tmp for the UMLs, so that we can operate
	# without syslogd, which seems to break on UMLs
	logfile=/tmp/pluto.log
	logtime=no
	logappend=no
	plutodebug=all
	dumpdir=/tmp
	protostack=netkey
	# typo on purpose below - it should get ignored and not fail connection
	virtual-private=%v4:192.1.2.024

conn xauth-road-eastnet-psk
	ikev2=no
	also=road-eastnet
	auto=ignore
	rightxauthserver=yes
	leftxauthclient=yes

conn road-eastnet-psk
	also=eastnet-ipv4
	also=road-east-psk
	auto=ignore
	left=%any
	ike=3des-sha1
	aggressive=yes
	authby=secret

conn road-east-psk
	ikev2=no
	right=192.1.2.23
	rightid=@east
	rightnexthop=192.1.2.45
	leftsubnet=vhost:%priv,%no
	leftid=@roadrandom
	leftnexthop=192.1.3.254

include	/testing/baseconfigs/all/etc/ipsec.d/ipsec.conf.common
