config setup
	plutodebug="all"
	logtime=no
	logappend=no
	logfile=/tmp/pluto.log
	dumpdir=/var/tmp
	protostack=netkey

conn %default
        ikev2=insist

include /testing/baseconfigs/all/etc/ipsec.d/ipsec.conf.common
