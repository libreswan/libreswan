Basic pluto X.509 test from x509-pluto-01, but now
west is dropped UDP fragments, so the default policy for
using IKE fragments should kick in when we don't get a response
to our MAIN_I3 packet. We re-send it in IKE fragments, which east
assembles and the IKE gets established successfully.

