From: Daniel Kahn Gillmor <dkg@fifthhorseman.net>
Date: Wed, 25 Jan 2017 19:13:15 -0500
Subject: two minor spelling errors

---
 programs/configs/d.ipsec.conf/seccomp.xml | 2 +-
 programs/pluto/whack.c                    | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/programs/configs/d.ipsec.conf/seccomp.xml b/programs/configs/d.ipsec.conf/seccomp.xml
index e5eecc446..cbb5e480c 100644
--- a/programs/configs/d.ipsec.conf/seccomp.xml
+++ b/programs/configs/d.ipsec.conf/seccomp.xml
@@ -14,7 +14,7 @@ AppArmor or other OS security features.</para>
 
 <para>The current default is disabled, but it is expected that in
 the future this feature will be enabled on all supported operating
-systems. Similarly, it is expected that further priviledge separation
+systems. Similarly, it is expected that further privilege separation
 will reduce the allowed syscalls - for example for the crypto helpers
 or DNS helpers.</para>
 
diff --git a/programs/pluto/whack.c b/programs/pluto/whack.c
index 0b71475d6..49a1bcd1f 100644
--- a/programs/pluto/whack.c
+++ b/programs/pluto/whack.c
@@ -2160,7 +2160,7 @@ int main(int argc, char **argv)
 	 */
 	if (msg.sa_rekey_fuzz > INT_MAX - 100 ||
 	    deltasecs(msg.sa_rekey_margin) > (time_t)(INT_MAX / (100 + msg.sa_rekey_fuzz)))
-		diag("rekeymargin or rekeyfuzz values are so large that they cause oveflow");
+		diag("rekeymargin or rekeyfuzz values are so large that they cause overflow");
 
 	check_life_time(msg.sa_ike_life_seconds, IKE_SA_LIFETIME_MAXIMUM,
 			"ikelifetime", &msg);
