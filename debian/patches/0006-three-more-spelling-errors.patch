From: Daniel Kahn Gillmor <dkg@fifthhorseman.net>
Date: Sun, 7 Aug 2016 20:43:56 -0400
Subject: three more spelling errors

---
 lib/libbsdpfkey/pfkey_dump.c               | 2 +-
 programs/newhostkey/ipsec_newhostkey.8.xml | 2 +-
 programs/pluto/connections.c               | 2 +-
 programs/pluto/ikev2_parent.c              | 2 +-
 programs/pluto/plutoalg.c                  | 2 +-
 5 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/lib/libbsdpfkey/pfkey_dump.c b/lib/libbsdpfkey/pfkey_dump.c
index f34afcd..dcf42dc 100644
--- a/lib/libbsdpfkey/pfkey_dump.c
+++ b/lib/libbsdpfkey/pfkey_dump.c
@@ -201,7 +201,7 @@ static struct val2str str_alg_comp[] = {
 };
 
 /*
- * dump SADB_MSG formated.  For debugging, you should use kdebug_sadb().
+ * dump SADB_MSG formatted.  For debugging, you should use kdebug_sadb().
  */
 void pfkey_sadump(m)
 struct sadb_msg *m;
diff --git a/programs/newhostkey/ipsec_newhostkey.8.xml b/programs/newhostkey/ipsec_newhostkey.8.xml
index d24551e..5c6c346 100644
--- a/programs/newhostkey/ipsec_newhostkey.8.xml
+++ b/programs/newhostkey/ipsec_newhostkey.8.xml
@@ -56,7 +56,7 @@
 	<term><option>--output</option>&nbsp;<replaceable>filename</replaceable></term>
 	<listitem>
 	  <para>The <option>--output</option> option specifies an
-	  <emphasis remap='I'>ipsec.secrets</emphasis> formated file
+	  <emphasis remap='I'>ipsec.secrets</emphasis> formatted file
 	  (see
 	  <citerefentry><refentrytitle>ipsec.secrets</refentrytitle><manvolnum>5</manvolnum></citerefentry>).
 	  to store the public key information.  If the file does not
diff --git a/programs/pluto/connections.c b/programs/pluto/connections.c
index 362fca3..8e0ef66 100644
--- a/programs/pluto/connections.c
+++ b/programs/pluto/connections.c
@@ -524,7 +524,7 @@ static err_t default_end(struct end *e, ip_address *dflt_nexthop)
  * Format the topology of a connection end, leaving out defaults.
  * Largest left end looks like: client === host : port [ host_id ] --- hop
  * Note: if that == NULL, skip nexthop
- * Returns strlen of formated result (length excludes NUL at end).
+ * Returns strlen of formatted result (length excludes NUL at end).
  */
 size_t format_end(char *buf,
 		size_t buf_len,
diff --git a/programs/pluto/ikev2_parent.c b/programs/pluto/ikev2_parent.c
index 1da001a..768afdd 100644
--- a/programs/pluto/ikev2_parent.c
+++ b/programs/pluto/ikev2_parent.c
@@ -1912,7 +1912,7 @@ static stf_status ikev2_verify_and_decrypt_sk_payload(struct msg_digest *md,
 
 		/* note: no iv is longer than MAX_CBC_BLOCK_SIZE */
 		unsigned char enc_iv[MAX_CBC_BLOCK_SIZE];
-		construct_enc_iv("decription IV/starting-variable", enc_iv,
+		construct_enc_iv("decryption IV/starting-variable", enc_iv,
 				 wire_iv_start, salt,
 				 pst->st_oakley.encrypter);
 
diff --git a/programs/pluto/plutoalg.c b/programs/pluto/plutoalg.c
index 8e2fd48..f9f609c 100644
--- a/programs/pluto/plutoalg.c
+++ b/programs/pluto/plutoalg.c
@@ -393,7 +393,7 @@ void alg_info_snprint_ike(char *buf, size_t buflen,
 		    ike_alg_hash_present(ike_info->ike_halg) &&
 		    lookup_group(ike_info->ike_modp) != NULL) {
 			if (strlen(sep) >= buflen) {
-				DBG_log("alg_info_snprint_ike: buffer too short for seperator");
+				DBG_log("alg_info_snprint_ike: buffer too short for separator");
 				break;
 			}
 			strcpy(ptr, sep);
